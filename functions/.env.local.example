# Copy to .env.local (gitignored) and fill in. Loaded automatically by the
# Functions emulator. Only needed once Phase 3 (real generation) is wired
# up — not required for local dev before that.

ANTHROPIC_API_KEY=sk-ant-...
