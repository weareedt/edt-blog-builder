/**
 * The article byline's "Author" is always this fixed value — never a form
 * field, never in any content schema, never something the model produces.
 */
export const ARTICLE_AUTHOR = 'The EDT Team';

/** The byline's "Published" value, matching what every template already shows. */
export const ARTICLE_PUBLISHED_LABEL = 'EDT Blog';

/** ~ words per minute used to cross-check the model's readTimeMinutes estimate. */
export const READING_WPM = 220;
